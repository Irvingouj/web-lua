// Host call handling — legacy host handler bridge

// This module is imported for side effects to ensure host handler
// registration helpers are available via the runtime module.
// No additional tool registrations here.
